[package]
name = "source"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib", "lib"]
name = "source"

[dependencies]
name = { path = "../name" }
serde = {version="1.0", features=["derive"]}
uniffi = "0.31"
